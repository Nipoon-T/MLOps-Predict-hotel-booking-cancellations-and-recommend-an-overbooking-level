# Data Decisions

เอกสารนี้บันทึกการตัดสินใจเกี่ยวกับข้อมูลผิดปกติ ข้อมูลซ้ำ และค่าว่างที่ใช้ในขั้นตอน Data Cleaning และเตรียมข้อมูลสำหรับงานถัดไป

## 1. Summary

| ปัญหา                  |                             จำนวน | การตัดสินใจ                        | เหตุผล                                                                                             |
| ---------------------- | --------------------------------: | ---------------------------------- | -------------------------------------------------------------------------------------------------- |
| ADR ติดลบ              |                                 1 | ตัดออก                             | ค่า ADR ติดลบไม่สอดคล้องกับความหมายของราคา                                                         |
| ผู้เข้าพักเป็นศูนย์    |                               180 | ตัดออก                             | ไม่สอดคล้องกับข้อมูลการจองห้องพักที่ต้องมีผู้เข้าพัก                                               |
| แถวซ้ำ                 | 40,141 flagged / 31,980 duplicate | ไม่ลบ และสร้าง `is_duplicate` flag | 40,141 คือทุกแถวที่อยู่ในกลุ่มข้อมูลซ้ำ ส่วน 31,980 คือ duplicate rows ที่ไม่นับ occurrence แรก    |
| `country` เป็นค่าว่าง  |                               478 | เก็บไว้ก่อน                        | จัดการในขั้นตอน feature engineering โดยใช้ flag หรือ `Unknown` ตามการตัดสินใจของบทบาทที่ทำ feature |
| `agent` เป็นค่าว่าง    |                            16,280 | เก็บไว้ก่อน                        | จัดการในขั้นตอน feature engineering โดยใช้ flag หรือ `Unknown` ตามการตัดสินใจของบทบาทที่ทำ feature |
| `company` เป็นค่าว่าง  |                           112,441 | เก็บไว้ก่อน                        | จัดการในขั้นตอน feature engineering โดยใช้ flag หรือ `Unknown` ตามการตัดสินใจของบทบาทที่ทำ feature |
| `children` เป็นค่าว่าง |                                 4 | เก็บไว้ก่อน                        | จัดการในขั้นตอน feature engineering โดยใช้ flag หรือ `Unknown` ตามการตัดสินใจของบทบาทที่ทำ feature |
| ADR สูงสุด             |                             5,400 | ยังไม่ตัดหรือ clip                 | ค่าสูงสุดยังต่ำกว่า validation upper bound ที่กำหนดไว้ 6,000 จึงยังไม่ตัดออกในขั้น clean           |
| อัตราการตัดข้อมูล      |           181 / 119,390 = 0.1516% | อนุญาตให้ดำเนินการต่อ              | ต่ำกว่า safety threshold 1% ที่กำหนดไว้ใน `clean_data.py`                                          |

## 2. Data Cleaning Rules

### 2.1 Negative ADR

พบข้อมูลที่มี `adr < 0` จำนวน 1 แถว

การตัดสินใจ:

* ลบแถวที่มี ADR ติดลบ
* ไม่ทำการแทนค่าด้วยค่าเฉลี่ยหรือ median
* เหตุผลคือค่า ADR ติดลบไม่สอดคล้องกับความหมายของราคา

### 2.2 Zero Guests

พบข้อมูลที่มีจำนวนผู้เข้าพักรวมเป็นศูนย์จำนวน 180 แถว

การตัดสินใจ:

* ลบแถวที่มีจำนวนผู้เข้าพักรวมเท่ากับศูนย์
* เหตุผลคือเป็นข้อมูลที่ไม่สอดคล้องกับตรรกะของการจองห้องพักที่มีผู้เข้าพัก

### 2.3 Duplicate Rows

หลังจาก cleaning พบแถวที่อยู่ในกลุ่มข้อมูลซ้ำจำนวน **40,141 แถว** เมื่อใช้การตรวจแบบ `duplicated(keep=False)` ซึ่งหมายถึงทุกแถวที่มีข้อมูลซ้ำกับแถวอื่น

เมื่อใช้การตรวจแบบ `duplicated(keep='first')` พบจำนวน **31,980 แถว** ที่เป็น duplicate โดยไม่นับ occurrence แรก ซึ่งตรงกับจำนวนที่รายงานใน reviewer

การตัดสินใจ:

* ไม่ลบ duplicate rows
* เพิ่มคอลัมน์ `is_duplicate`
* ใช้ flag เพื่อให้ขั้นตอน modeling และ decision layer สามารถทดลองผลกระทบของ duplicate ได้

การตัดสินใจนี้สอดคล้องกับแนวทาง reviewer ที่เสนอให้เก็บข้อมูลซ้ำไว้และให้บทบาท modeling ทดลองทั้งสองกรณี

> หมายเหตุ: ค่า 40,141 แถวหมายถึงจำนวนทุกแถวที่อยู่ในกลุ่ม duplicate (`duplicated(keep=False)`)

> ค่า 31,980 แถวหมายถึงจำนวน duplicate rows โดยไม่นับ occurrence แรก (`duplicated(keep='first')`)

## 3. Missing Values

Missing values ใน `country`, `agent`, `company` และ `children` จะไม่ถูกลบออกในขั้นตอน cleaning

การจัดการจะดำเนินการในขั้น feature engineering โดยพิจารณา:

* สร้าง missing-value flag
* หรือแทนค่าด้วย `Unknown` ในกรณีที่เหมาะสม

เหตุผลคือ missing values ไม่ได้หมายความว่าแถวนั้นเป็นข้อมูลผิดโดยอัตโนมัติ และ reviewer ระบุว่าค่าว่างเหล่านี้ควรได้รับการตัดสินใจในขั้น feature engineering

จำนวนที่พบหลัง cleaning:

| Column     | Missing |
| ---------- | ------: |
| `country`  |     478 |
| `agent`    |  16,280 |
| `company`  | 112,441 |
| `children` |       4 |

## 4. ADR Upper Bound

ค่า ADR สูงสุดที่พบในข้อมูลคือ:

```text
5400.0
```

ค่า ADR สูงสุดดังกล่าวยังต่ำกว่า validation upper bound ที่กำหนดไว้ที่ `6000` ดังนั้นจึงไม่มีการตัดหรือ clip ค่า ADR ในขั้นตอน Data Cleaning

## 5. Removal Safety Threshold

ข้อมูลต้นฉบับมีทั้งหมด 119,390 แถว และมีการตัดข้อมูลออกทั้งหมด 181 แถว ได้แก่:

* ADR ติดลบ: 1 แถว
* ผู้เข้าพักเป็นศูนย์: 180 แถว

คิดเป็นอัตราการตัดข้อมูล:

```text
181 / 119,390 = 0.1516%
```

อัตราดังกล่าวต่ำกว่า safety threshold ที่กำหนดไว้ที่ 1% ใน `clean_data.py` จึงอนุญาตให้ pipeline ดำเนินการต่อได้

หากอัตราการตัดข้อมูลสูงกว่า 1% pipeline จะหยุดและแจ้งข้อผิดพลาดเพื่อป้องกันการสูญเสียข้อมูลจำนวนมากโดยไม่ได้ตรวจสอบเพิ่มเติม
